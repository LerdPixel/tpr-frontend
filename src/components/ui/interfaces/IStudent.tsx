export interface IStudent {
    created_at : string,
    email : string,
    first_name : string,
    group_id : number,
    id : number,
    is_approved : boolean,
    last_name : string,
    patronymic : string,
    role_id : number
}